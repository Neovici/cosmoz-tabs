---
'@neovici/cosmoz-tabs': minor
---

Add a `segmented` tab variant — Untitled UI's "button border" style: a filled track with a 1px ring holding a raised, selected pill. Works on both `cosmoz-tabs` and `cosmoz-tabs-next`, and the track hugs its tabs when combined with `compact-width`.
