---
engines:
  eslint:
    enabled: true
    channel: "eslint-4"
    checks:
      complexity:
        enabled: false
    config:
      sanitize_batch: false
      extensions:
       - .js
       - .html

  duplication:
    enabled: true
    config:
      languages:
        javascript:
          mass_threshold: 50

  fixme:
    enabled: true

ratings:
  paths:
  - "**.js"
  - "**.html"
